language: python

python:
  - "2.6"
  - "2.7"
  - "3.3"

install:
  - pip install . --quiet --use-mirrors
  - pip install nose --quiet --use-mirrors

script: nosetests

branches:
  only:
    - master
